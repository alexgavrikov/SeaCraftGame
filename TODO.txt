ПОМЕНЯЛ СХЕМУ ОТПРАВКИ СООБЩЕНИЙ КЛИЕНТАМ. ТЕПЕРЬ ДЕЛАЕМ

Если кто-то выбрал задачу, пусть оставит под ней своё имя, чтобы дать другим понять, что она занята.

1. Реализовать TClient::GetShooting(const size_t x_coord, const size_t y_coord)
(Объявлена в файле TClient.h. Пустая реализация в TClient.cpp)
Сурен.

2. Реализовать TClient::CorrectShips() const
(Объявлена в файле TClient.h. Пустая реализация в TClient.cpp)
Сурен.

3. Добавить кое-что в реализацию функции TClient::Send()
А именно оборачивание сообщения data в HTML-хэдеры
НАДО

4. Добавить кое-что в реализацию функции Server::RecieveShips(char* buf,
                          int size,
                          Clients::iterator client_iterator)
А именно распарсить buf, понять, о получении кораблей ли это сообщение, и если да, то
выловить оттуда расстановку, записать её в ships. Если сообщение не об этом, то return false

5. Добавить кое-что в реализацию функции Server::RecieveStep(char* buf,
                          int size,
                          Clients::iterator client_iterator)
А именно распарсить buf, понять, о получении хода ли это сообщение, и если да, то
выловить оттуда ход. Если сообщение не об этом, то return false

6. Server::AcceptLoop - там возможно надо пару мест изменить, связанных с отправкой HTML-страницы клиенту
Места помечены так: CODE HERE
НАДО

